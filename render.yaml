services:
  - type: web
    name: chat-room-api
    env: node
    plan: free
    buildCommand: npm run build:production
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
    healthCheckPath: /health
